<div class="border border-3 rounded-3 p-1 bg-light my-1">
    <%= form_with model: atteendee do |f| %>
    <%= f.text_field :status, value: 'acepted' %>
    <div class="d-flex justify-content-center">
        <%= f.submit 'Accept', class:'btn btn-success' %>
    </div>   
<% end %>
</div>