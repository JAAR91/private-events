<div class="container">
    <div class="row">
        <div class="col-3">

        </div>
        <div class="col-6">
            <div class="container py-2 my-1 text-center">
                <%= event_post(current_user) %>
            </div>
            <% @event.order('created_at DESC').each do |event| %>
                <div class="border border-3 rounded p-4 bg-light my-1">
                    <div class="shadown m-1 d-flex border-bottom">
                        <p class="text-uppercase"> <%= event_title(logged_in?, event) %> </p>
                    </div>
                    <div class="container shadown">
                            <p class="text-break"><%= event.description %></p>
                    </div>
                    <p class="text-break"><%= event.date %></p>
                    <p class="shadown ms-auto text-end"><%= event.tpeople%></p>
                </div>
            <% end %>
        </div>
        <div class="col-3">

        </div>
    </div>
</div>